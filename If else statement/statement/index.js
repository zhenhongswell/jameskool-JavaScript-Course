const a = 3;
const b = 2;

if (a > b) {
  console.log(`a:${a} is bigger than b:${b}`);
} else if (a === b) {
  console.log(`a:${a} is equals to b:${b}`);
} else {
  console.log(`a:${a} is lesser than b:${b}`);
}

// else if{}
//
// equals
//
// else
// {
// if ()
// }
